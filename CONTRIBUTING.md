# Contributing

Internal Coral Platform repo. Open a PR against `main` with:

- A short description in the PR template  
- Passing CI (`npm test`, `npm run lint`)  
- No secrets in diffs — use `.env.example` for new variables  

Release cadence: weekly staging deploys, monthly production (owned by `#fleet-platform`).
